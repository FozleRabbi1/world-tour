// import { useState } from 'react'
import './App.css'

function App() {
  // const [color, setColor] = useState(false);

  return (
    <div>
    {/* <div className={`bg-style ${color ? "colorDark" : "colorWhite"}`} > */}

      {/* <button onClick={() => setColor(!color)} > {color ? "dddd" : "wwww"} </button> */}

    </div>
  )
}

export default App
